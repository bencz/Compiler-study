//выражения, не содержащие управляющие конструкции, операции вывода
//не зависящие от переменных, изменяющихся в цикле и не изменяющие
//переменных, от которых зависят другие выражения в цикле можно вынести
//из тела цикла
var
  i: Integer;
begin
  i := 10;
  while (i) do begin
    i := i - 1;
    5;
  end;
end.