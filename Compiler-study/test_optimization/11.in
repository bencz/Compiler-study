//массив считается за 1 переменную, обращение к разным ячейкам
//массива считаются просто за обращение к массиву.
var
  i, j, k : Integer;
  a, b, c, d: array[1..10, 1..10] of Integer;
  
begin
  for i := 1 to 10 do
    begin
      for j := 1 to 10 do
        begin
          d[0][0] := 4;
          d[j][0] := 5;
          d[i][0] := k;
        end;
    end;
end.
