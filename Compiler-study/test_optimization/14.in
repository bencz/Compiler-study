var
  i, j, k : Integer;
  a, b, c, d: array[1..10, 1..10] of Integer;
  
begin
  for i := 1 to 10 do
    begin
      for j := 1 to 10 do
        begin
          d[i][0] := 5;
          a[i][i] := d[i][i];
        end;
    end;
end.
